services:
  - type: web
    name: ai-sql-studio
    env: python
    buildCommand: pip install -r requirements.txt && python sql.py
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: API_KEY
        sync: false
